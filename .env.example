# Copy this file to ".env.local" and set your local vault path.
# ".env.local" is ignored by git.

# Vault root folder (not the plugin folder).
EMERA_VAULT_PATH=/mnt/c/Users/<you>/WinProjects/obsidian/<your-vault>

# Optional, defaults to "emera".
EMERA_PLUGIN_ID=emera
